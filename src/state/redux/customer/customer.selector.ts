export const customersSelector = (state: any) => state.customer.customers;

export const currentCustomerSerlector = (state: any) => state.customer.currentCustomer;

export const doSearchSelector = (state: any) => state.customer.doSearch;

export const searchTypeSelector = (state: any) => state.customer.searchType;

export const filtersSelector = (state: any) => state.customer.filters;

export const tempOrdersSelector = (state: any) => state.customer.tempOrders;